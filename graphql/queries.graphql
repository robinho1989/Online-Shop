query GetProducts {
	products {
		description
		name
		price
		images {
			fileName
			url
		}
		slug
	}
}
query GetProductDetailsBySlug($slug: String) {
  products(where: { slug: $slug }) {
    name
    price
    images {
      url
    }
  }
}
