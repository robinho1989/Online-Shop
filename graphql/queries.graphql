query GetProducts {
	products {
		id
		description
		name
		price
		images {
			fileName
			url
		}
		slug
	}
}
query GetProductDetailsBySlug($slug: String) {
	products(where: { slug: $slug }) {
		name
		price
		images {
			url
		}
	}
}
